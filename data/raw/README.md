# Data folder

Repo mặc định dùng IMDB thông qua Hugging Face `datasets`.

File `sample_imdb_tiny.csv` chỉ dùng cho smoke test, CI và kiểm tra nhanh pipeline. Không dùng file nhỏ này để kết luận mô hình tốt hay không tốt.
