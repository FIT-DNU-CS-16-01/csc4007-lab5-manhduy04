# Notebooks

Sinh viên có thể tạo notebook cá nhân để quan sát dữ liệu hoặc vẽ thêm biểu đồ.

Tuy nhiên, bài nộp chính nên chạy được bằng `run_lab5.py`, vì CI và giảng viên sẽ kiểm tra repo qua script này.
